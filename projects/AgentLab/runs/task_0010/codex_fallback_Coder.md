# Coder Handoff to External IDE AI

Provider: external_ide_ai
Model: External AI

CLI blocked automatic Coder execution because the default Coder profile
is `external_ide_coder`. The external IDE AI (Codex Plus) should manually
take over the Coder stage.

To use API coding fallback, re-run with `--provider qwen` or `--provider deepseek`.
