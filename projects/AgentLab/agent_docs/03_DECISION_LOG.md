# Decision Log

## 2026-05-29 - task_0001

- Build the first UI as static files under `web_ui/`.
- Keep runtime, configuration, project memory, and UI files separated.
- Show all registered agents, including skipped support agents, so the board reflects the complete team.
- Use a sample JSON snapshot now and leave a clear path to a local status endpoint later.
