version: 1

storage:
  mode: local_first
  workspace_root: projects
  project_root: projects/<ProjectName>
  authoritative_task_state: runs/task_xxxx
  authoritative_project_memory: project_brain
  authoritative_current_artifacts: production_and_project_artifact_index
  operator_memory: agent_docs
  task_evidence: runs/task_xxxx
  derived_knowledge_index: .agentlab_runtime/knowledge
  server_sync: opt_in_versioned_ssh_relay
  github_private_backup: planned_local_first_manual_sync
  workspace_harness_map: AGENTS.md

records:
  workspace_knowledge:
    - AGENTS.md
    - OPERATING_MODEL.md
    - _shared/AGENT_PROTOCOL.md
    - config/harness_policy.yml
    - config/brain_governance.yml
    - config/validation_gates.yml
    - config/knowledge_system.yml

  task_state:
    - user_request.md
    - supervisor_plan.md
    - reposcout_report.md
    - implementation_report.md
    - validation_report.md
    - audit_report.md
    - archive_update.md
    - workflow_plan.yml
    - cost_ledger.yml
    - brain_decisions.yml
    - USER_DECISION_REQUIRED.md

  project_memory:
    - 00_CONTEXT_PACK.md
    - 01_REPO_MAP.md
    - 02_TASK_LEDGER.yml
    - 03_DECISION_LOG.md
    - 04_INTERFACE_REGISTRY.md
    - 05_CHANGELOG_AGENT.md
    - 06_RISK_REGISTER.md
    - 07_DEVELOPMENT_LOG.md
    - 08_WORKER_DIALOGUE_LOG.md
    - 09_COST_LEDGER.yml
    - 10_SYNC_LEDGER.yml

drift_controls:
  - Start every task from project memory and active task state, not chat history alone.
  - Keep long context compressed into reports before passing it to later agents.
  - Record assumptions and unresolved risks before implementation.
  - Update project memory only from validated reports.
  - Never claim a test, build, or manual verification passed without evidence.
  - Trigger AgentLab only when the user explicitly asks to use AgentLab.
  - Record every AgentLab team action in the project development log.
  - Record assigned worker dialogue summaries and implementation actions in the dialogue log.
  - Record provider/model usage from receipts; unknown subscription usage remains unknown.
  - Require safe full path/metadata inventory before repository work; it is exempt from traversal approval.
  - Require a brain traversal request before any bulk full-directory or full-repository content read.
  - Read or refresh canonical PROJECT_HANDOFF.md before project content reads; legacy handoff names are read-only.
  - Stop and replan when repeated loops or task-goal drift are detected.
  - Treat AGENTS.md as a map into local memory and config, not a full duplicate of policy.
  - Promote repeated user feedback or audit findings into harness_policy, validation gates, scripts, or concise project memory.
  - Run harness-status before relying on stale project memory for planning.
