task_id: task_p2g_smoke
delivery_id: fake_repo
provider_id: mock_executor
executor: mock
created_at: '2026-06-14T05:55:16Z'

cost_compliance:
  estimated_cost_usd: 0.0
  cost_mode: dry_run
  within_budget: true
  budget_limit_usd: 0.50
  notes:
    - "Mock executor incurs no real cost."
    - "Dry-run mode guarantees zero API spend."

resource_compliance:
  compute_resources: mock
  network_calls: false
  external_apis: false
  secrets_accessed: false
  notes:
    - "All operations are local and deterministic."

evidence_compliance:
  artifacts_provided:
    - README.md (present but incomplete)
    - review_report.md (generated by 3E reviewer)
    - review_report.yml (generated by 3E reviewer)
    - review_verdict.yml (generated by closure)
  artifacts_missing:
    - external_handoff.md (required by review policy)
    - skill_usage_ledger.yml (required by review policy)
  report_sections_missing:
    - Summary
    - Tests Run
    - Safety Evidence
    - Known Limitations
    - Verdict
  evidence_score: 0.3

governance_decision:
  status: WATCHLIST
  recommendation: neutral
  reasons:
    - "Delivery rejected due to missing required artifacts."
    - "Quality score 0.4 is below prefer threshold."
    - "No safety violations — clean dry-run execution."

watchlist_triggered: false
quarantine_triggered: false
future_routing_notes:
  - "mock_executor is suitable for local dry-run tasks."
  - "For production tasks requiring real API calls, route to approved external executor."
  - "This task type (repo consistency check) should default to dry-run until delivery quality improves."
