schema_version: 1
status: blocked
scope: crown_formal_live_narrative_eval
attempted_action: rerun the one-chapter live narrative-eval outside the sandbox
reason: >
  The approval system rejected the non-sandbox retry because it would send
  Crown_of_Ash project context and manuscript-governance data to the external
  DeepSeek API. The live failure is therefore retained as a provider/privacy
  boundary until the user explicitly approves this private-context egress.
safe_next_steps:
  - use mock/governance acceptance for local deterministic checks
  - run live prose only after explicit user approval for external API context egress
  - keep candidate outputs out of production until heavy audit and user promotion
