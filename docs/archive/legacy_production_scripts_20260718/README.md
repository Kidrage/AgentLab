# Retired Project-Specific Production Scripts

These scripts are historical and must not be executed as AgentLab production
entrypoints.

`write_chapters.py` bypassed current governance by mutating workflow/lifecycle
files, auto-approving recovery, reading rebuild trees as authority, and writing
directly to an old manuscript directory. `reader_server.py` was a project-specific
Crown reader with no active framework registration.

Current longform work must use mission routing, narrative production packs,
run-local candidates, structured memory, heavy-audit gates, and explicit artifact
promotion.
