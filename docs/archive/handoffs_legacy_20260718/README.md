# Legacy Mainline Handoffs

These documents describe superseded S0-S12 and M-series repair plans. Their
closed-work details remain useful as historical context, but their cursors and
provider/RAG assumptions are not current authority.

`PROJECT_HANDOFF_PRE_PRUNING_20260718.md` preserves the stale embedded R0-R5
repair program that previously occupied the root handoff. It is historical
analysis only and must not block current work.

Use these live files instead:

- `PROJECT_HANDOFF.md` for the generated cross-agent repository handoff.
- `.codex/MAINLINE.md` for the compact active mainline.
- `CONTEXT.md` for the current local working context.
